# Change Log

### v0.1.1

- Add keybinding `Ctrl+Shift+D` for `vscode-git-diff.openChange` command

### v0.1.0

- Init project
- Add `vscode-git-diff.openChange` command : Open Git Diff with commit selection
